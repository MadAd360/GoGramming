[uwsgi]
#application's base folder
base = /home/pi/application/gocompile/app

#python module to import
app = run
module = %(app)

#home = /home/pi/application/gocompile/flask
pythonpath = /home/pi/application/gocompile
venv = /home/pi/application/gocompile/flask

#socket file's location
socket = /home/pi/application/gocompile/flask/%n.sock

#permissions for the socket file
chmod-socket    = 666

#the variable that holds a flask application inside the module imported at line #6
callable = app

#location of log files
logto = /var/log/uwsgi/%n.log
